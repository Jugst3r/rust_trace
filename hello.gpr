with "gnatcov_rts_full" ;

library project Hello is
   for Library_Name use "bindings";
   for Library_Dir use "c_and_ada_bindings";
   for Source_Dirs use ("src");
   for Languages use ("Ada", "C");
   for Object_Dir use "obj";
end Hello;
